services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - LOG_FILE_PATH=/DATA/data-viewer/log/app.log
      - LOG_LEVEL=INFO
    volumes:
      - ./logs/backend:/DATA/data-viewer/log

  frontend:
    build:
      context: ./frontend
    ports:
      - "9000:80"
    depends_on:
      - backend
    restart: unless-stopped 
