const config = {
  api: {
    baseUrl: 'http://localhost:8000',
    endpoints: {
      init: '/init',
      query: '/query'
    }
  },
  viewer: {
    width: 1024,
    pageSize: 10
  }
}

export default config 